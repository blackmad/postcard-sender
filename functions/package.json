{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && HOST=http://localhost:3000 GOOGLE_APPLICATION_CREDENTIALS=../creds/political-postcards-firebase-adminsdk.json firebase emulators:start --only functions",
    "shell": "npm run build &&  GOOGLE_APPLICATION_CREDENTIALS=../creds/political-postcards-firebase-adminsdk.json firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0",
    "joi-extract-type": "^15.0.2",
    "lob": "^6.1.0",
    "mailgun-js": "^0.22.0",
    "stripe": "^8.60.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.1.6",
    "typescript": "3.8.3"
  },
  "private": true
}
